#include<stdio.h>
#include<stdlib.h>

int main(int argc, char *argv[]){
	int a,b,c,d;
	scanf("%d %d %d", &a, &b, &c);
	d = (a+b+abs(a-b))/2;
	d = (c+d+abs(c-d))/2;
	d = d/3;
	
	printf("%d eh o maior\n", d);
}
