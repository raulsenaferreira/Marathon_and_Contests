#include<stdio.h>
#include<stdlib.h>

int main(int argc, char *argv[]){
	char* nome;
	float salario, totalVenda, comissao;

	scanf("%s", &nome);
	scanf("%f", &salario);
	scanf("%f", &totalVenda);
	
	comissao = salario+(0.15*totalVenda);
	
	printf("TOTAL = R$ %.2f\n",comissao);
}
