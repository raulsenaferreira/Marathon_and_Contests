#include <iostream>
#include <cstdio>
using namespace std;

int main(int argc, char const *argv[])
{
	int r1, r2, num;
	cin >> num;
	for (int i = 0; i < num; i++)
	{
		scanf("%d %d", &r1, &r2);
		int sum = r1 + r2;
		cout << sum << endl;
	}
	return 0;
}